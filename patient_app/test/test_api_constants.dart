/// Base URL for [Dio] + [http_mock_adapter] in widget/repository tests.
///
/// Must match the deployed API host (path prefix `/v1`).
const kTestApiV1BaseUrl =
    'https://gpsmedicalgo-production.up.railway.app/v1';
